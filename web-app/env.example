# Copy to .env.local and adjust as needed. Defaults (see src/lib/config.ts)
# already point at the current testnet deployment, so this file exists for
# documentation and local overrides, not because the app requires it.

WARDEN_RPC_URL=https://soroban-testnet.stellar.org
WARDEN_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
WARDEN_DEMO_ACCOUNT_ID=CCBXMG3RJUQUW6RNONDSLQUKL6P7YIYR6T56BCXLECMPA3TVCASAABOG

# warden-backend's indexer -- see that repo for how to run it locally
NEXT_PUBLIC_INDEXER_URL=http://localhost:8080
